all:test#rund by default
	-@echo all
	
test:hazem
	-@echo "test"

hazem:
	-@echo "hello hazem"


#callec directly from the main makefile that is been generated with CCS but 
#we need to disable that step after the makefile first build

# test:
# 	-@make -f makefile_Tests